# InventoryCountingAi_model_Website
for CS461 Final Project
🛠️ เทคโนโลยีและไลบรารีที่ใช้

องค์ประกอบ

รายละเอียด

Model Architecture

YOLOv8s (You Only Look Once, Version 8 - Small)

Frameworks

PyTorch / Ultralytics (Model), Flask (Backend API), HTML/CSS/JS (Frontend)

Deployment

Gunicorn (สำหรับรัน Production Server), Netlify (สำหรับ Frontend)

Dataset

SKU110k (เน้นความหนาแน่นของวัตถุสูง)

📦 โครงสร้างไฟล์ใน Repository

.
├── **best.pt** # ไฟล์โมเดล YOLOv8 ที่เทรนแล้ว (สำคัญมาก)
├── **app.py** # โค้ด Python สำหรับ Flask Web API (Backend)
├── **requirements.txt** # รายการ Libraries ที่จำเป็นสำหรับ Backend
├── **index.html** # โค้ดเว็บไซต์สำหรับอัปโหลดรูปภาพ (Frontend Demo)
├── **CS461_ShelfCounting_Outline.md** # โครงร่างสไลด์นำเสนอ
├── **Colab_Training.ipynb** # (ชื่อไฟล์ Colab, โปรดใส่ไฟล์นี้)
└── ... (ไฟล์อื่น ๆ เช่น reporting_script.py, data.yaml)


⚙️ วิธีการใช้งานและรันโค้ด (Setup & Run Commands)

โปรเจกต์นี้แบ่งออกเป็น 2 ส่วนหลัก: 1) การเทรนโมเดล (Colab) และ 2) การใช้งานจริง (Backend API & Frontend Web)

1. การเทรนโมเดลและการประเมินผล (Training & Evaluation)

ขั้นตอนเหล่านี้ดำเนินการใน Google Colab Notebook เพื่อใช้ GPU ในการประมวลผล:

Colab Link: https://colab.research.google.com/drive/1hy_kScyaJJ2n9DI1-3DLKtuUPaw79gq0

ขั้นตอน

คำสั่งที่เกี่ยวข้อง

ผลลัพธ์

1. Setup & Data

รันเซลล์ติดตั้ง Libraries และดาวน์โหลด/จัดเตรียม Dataset SKU110k

โฟลเดอร์ dataset_sku110k/ และไฟล์ data.yaml

2. Training

รันคำสั่ง model.train(...) เพื่อเทรน YOLOv8s

ได้ไฟล์ best.pt ที่มีความแม่นยำสูงสุด (mAP: 93.7%)

3. Evaluation

รัน reporting_script.py (หรือเซลล์ model.val())

แสดงตาราง Metrics (Precision 91.9%, Recall 87.5%)

4. Download

ดาวน์โหลดไฟล์ best.pt ที่เทรนแล้ว เพื่อนำไปใช้ในส่วน Backend

ไฟล์ best.pt

2. การติดตั้งและรัน Web API (Backend for Website)

เราใช้ Flask และ Gunicorn เพื่อรันโมเดล YOLOv8 เป็น API สำหรับให้เว็บไซต์เรียกใช้งาน

ข้อกำหนด: Python 3.8+ และ Libraries ใน requirements.txt

A. การติดตั้ง Dependencies

ใช้ไฟล์ requirements.txt ที่ให้มาในการติดตั้ง:

pip install -r requirements.txt


B. การรัน API Server

ให้แน่ใจว่าไฟล์ app.py และ best.pt อยู่ในโฟลเดอร์เดียวกัน จากนั้นรัน Gunicorn (แนะนำสำหรับ Production):

# รัน Flask App (app.py) ด้วย Gunicorn บนพอร์ต 5000
gunicorn app:app -w 4 -b 0.0.0.0:5000


API Endpoints:

Health Check: GET / -> Returns "YOLOv8 API is running!"

Inference (Detection & Counting): POST /detect

Input: Multi-part form data (file: image file)

Output: JSON { "detections": [...], "count": 42 }

3. การรัน Website (Frontend Demo)

เว็บไซต์ Frontend (Web Demo) ถูกสร้างไว้ในไฟล์ index.html ซึ่งใช้ JavaScript เพื่อส่งรูปภาพไปยัง Backend API (Deploy บน Render, AWS, หรือ Localhost)

URL Demo: https://smart-invertory-ai.netlify.app/ 

Local Run: เพียงเปิดไฟล์ index.html ด้วยเว็บเบราว์เซอร์

การเชื่อมต่อ:

ในโค้ด index.html ต้องมีการระบุ SERVER_ROOT_URL ให้ชี้ไปยังที่อยู่ของ API Server ที่กำลังรันอยู่ (เช่น http://localhost:5000 หรือ URL ของเซิร์ฟเวอร์ที่ Deploy จริง)
